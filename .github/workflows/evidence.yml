name: Evidence contract

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  doubt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./
        with:
          require-maps: "true"
          exclude: benchmarks/skill-portability/results/artifacts
